<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/style.css">
  <title>DBExplorer</title>
</head>

<body>
  <main>
    <h2>Todo database explorer</h2>
    <% if (items.length>= 1) { %>
      <p>The items in the list:</p>
      <ul>
        <% items.forEach(function(item){ %>
          <li>
            <%= item.text %> (id: <%= item.id %>, done: <%= item.done===0 ? 'no' : 'yes' %>)
          </li>
          <% }); %>
            <% } else { %>
              <p>This list has no items.</p>
              <% }; %>
      </ul>
  </main>
</body>

</html>
